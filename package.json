{
  "name": "vital-track-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-knowledge.js && cd web-app && npm install && npm run build"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1"
  }
}
